# OpenAPI export

The committed [`openapi.json`](https://github.com/jbwod/beampipe-core/blob/main/openapi.json) is the contract for Bruno, Read the Docs (ReDoc), and external clients.

## Regenerate

```bash
make openapi
```
