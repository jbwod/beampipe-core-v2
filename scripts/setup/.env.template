# =============================================================================
# Beampipe Core – Template Environment File
# =============================================================================

# ------------- app settings -------------
APP_NAME="Beampipe"
APP_DESCRIPTION="Beampipe Core"
APP_VERSION="0.1"
CONTACT_NAME=__PROMPT__
CONTACT_EMAIL=__PROMPT__
LICENSE_NAME="MIT"

# ------------- database -------------
POSTGRES_USER="postgres"
POSTGRES_PASSWORD=__GENERATE__
POSTGRES_SERVER="db"
POSTGRES_PORT=5432
POSTGRES_DB="beampipe"
POSTGRES_ASYNC_PREFIX="postgresql+asyncpg://"

# ------------- crypt -------------
SECRET_KEY=__GENERATE__
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=7

# ------------- admin -------------
ADMIN_NAME=__PROMPT__
ADMIN_EMAIL=__PROMPT__
ADMIN_USERNAME=__PROMPT__
ADMIN_PASSWORD=__PROMPT__

# ------------- redis -------------
REDIS_CACHE_HOST="redis"
REDIS_CACHE_PORT=6379
REDIS_QUEUE_HOST="redis"
REDIS_QUEUE_PORT=6379
REDIS_RATE_LIMIT_HOST="redis"
REDIS_RATE_LIMIT_PORT=6379

# ------------- ENV -------------
ENVIRONMENT=__ENVIRONMENT__
LOG_VERBOSITY="medium"

# ------------- basic -------------
WORKFLOW_EXECUTION_AUTOMATION_ENABLED=true
SHAPING_EXECUTION_MAX_IN_FLIGHT_RUNS=5
RESTATE_SLURM_REMOTE_POLL_INTERVAL_SECONDS=180
RESTATE_SLURM_REMOTE_POLL_MAX_ROUNDS=20

# ------------- api ratelimit -------------
# DEFAULT_RATE_LIMIT_LIMIT=10
# DEFAULT_RATE_LIMIT_PERIOD=3600

# ------------- logging -------------
# Allowed: full | medium (default) | minimal.
# LOG_VERBOSITY=full

# ------------- Restate S3 snapshots (optional) -------------
RESTATE_AWS_ACCESS_KEY_ID=__OPTIONAL__
RESTATE_AWS_SECRET_ACCESS_KEY=__OPTIONAL__

# ------------- CASDA / OPAL archive credentials (optional) -------------
CASDA_USERNAME=__OPTIONAL__
CASDA_PASSWORD=__OPTIONAL__
# CASDA_STAGE_BY_SBID=true

# ------------- Slurm SSH (optional) -------------
SLURM_SSH_KNOWN_HOSTS_SOURCE=./deploy/ssh/known_hosts
SLURM_SSH_PRIVATE_KEY_FILE=./deploy/ssh/id_slurm
# SLURM_SSH_CONNECT_TIMEOUT_SECONDS=30.0
# SLURM_SSH_COMMAND_TIMEOUT_SECONDS=60.0

# =============================================================================
# Global Pipeline tuning (advanced)
# - Project Modules will override Global Defaults 
# =============================================================================
#   WORKFLOW_EXECUTION_AUTOMATION_ENABLED=true
#   SHAPING_EXECUTION_MAX_IN_FLIGHT_RUNS=5
#   RESTATE_SLURM_REMOTE_POLL_INTERVAL_SECONDS=180
#   RESTATE_SLURM_REMOTE_POLL_MAX_ROUNDS=20
# =============================================================================

# ------------- discovery (DISCOVERY_*) -------------
# DISCOVERY_BATCH_SIZE=50
# DISCOVERY_BATCH_CONCURRENCY=5
# DISCOVERY_STALE_HOURS=24
# DISCOVERY_SCHEDULE_MINUTES=1
# DISCOVERY_TAP_TIMEOUT_SECONDS=120
# DISCOVERY_RETRY_COOLDOWN_MINUTES=60
# DISCOVERY_CLAIM_TTL_MINUTES=180
# DISCOVERY_MAX_SOURCES_PER_RUN=2000
# DISCOVERY_TAP_HEALTH_CHECK_ENABLED=true
# DISCOVERY_TAP_HEALTH_TIMEOUT_SECONDS=10.0

# ------------- shaping / backpressure (SHAPING_*) -------------
# SHAPING_ARQ_QUEUE_MAX_DEPTH=200
# SHAPING_DISCOVERY_MAX_IN_FLIGHT_BATCHES=4
# SHAPING_EXECUTION_MAX_IN_FLIGHT_RUNS=2
# SHAPING_ENQUEUE_PACING_MS=0.0
# SHAPING_DISCOVERY_MAX_BATCHES_PER_TICK=

# ------------- workflow engine selectors (WORKFLOW_*) -------------
# Pick "arq" for redis-queue execution or "restate" for durable workflows.
# WORKFLOW_ENGINE_EXECUTION=restate
# WORKFLOW_ENGINE_DISCOVERY=restate

# ------------- Restate ingress + retry policies (RESTATE_*) -------------
# RESTATE_INGRESS_BASE_URL=
# RESTATE_INVOKE_TIMEOUT_SECONDS=30.0
#
# ctx.run_typed retry policies — see https://docs.restate.dev/develop/python/durable-steps
# RESTATE_STEP_EXTERNAL_MAX_ATTEMPTS=3
# RESTATE_STEP_EXTERNAL_MAX_DURATION_MINUTES=45
# RESTATE_STEP_DB_MAX_ATTEMPTS=3
# RESTATE_STEP_DB_MAX_DURATION_MINUTES=15
# RESTATE_STEP_POLL_MAX_ATTEMPTS=3
# RESTATE_STEP_POLL_MAX_DURATION_MINUTES=5
# RESTATE_STEP_INITIAL_RETRY_SECONDS=2.0
# RESTATE_STEP_MAX_RETRY_INTERVAL_SECONDS=120.0
#
# Remote poll cadences (REST = e.g. CASDA async; Slurm = sbatch monitoring).
# RESTATE_REST_REMOTE_POLL_INTERVAL_SECONDS=15.0
# RESTATE_REST_REMOTE_POLL_MAX_ROUNDS=240
# RESTATE_SLURM_REMOTE_POLL_INTERVAL_SECONDS=30.0
# RESTATE_SLURM_REMOTE_POLL_MAX_ROUNDS=480

# ------------- Caching [unused] -------------
# CLIENT_CACHE_MAX_AGE=60